# DR Navigator — build roadmap

- [x] Types, demo drives, deterministic analysis engine
- [ ] Design tokens (dark navy professional theme)
- [ ] Store (localStorage seeded with demo drives) + settings context
- [ ] Shell: Sidebar (collapsible / mobile drawer), Header, breadcrumbs
- [ ] Components: StatCard, StatusBadge, DriveMap, GpsHealthCard, MotionTimeline,
      DriftChart, DriveTable, UploadDriveModal, AnalysisProgress, ReportCard, ModelCard,
      SystemConcept diagram
- [ ] Pages: Dashboard, Drives, DriveAnalysis, Reports, ReportDetail, Models, Settings
- [ ] Interactive map: MapLibre GL + OSM/Carto raster tiles, GPS path blue,
      DR path amber dashed, blackout zone red highlight, clickable event markers,
      zoom/pan, layer toggles, graceful SVG fallback when tiles fail
- [ ] Route head() metadata per page
- [ ] Verify build clean, no console errors
